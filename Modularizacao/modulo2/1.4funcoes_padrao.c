#include <stdio.h>

// Outras funções

/*
  putchar:
    Essa função envia um único caractere pela saída padrão
    (escreve apenas um caractere). Equivale ao comando: printf("%c",carac).
  Sintaxe: putchar();
*/

/*
  puts:
    Essa função envia uma string na saída padrão. A string
    será adicionada com um caractere '\n'. Equivale a: printf("%s\n", string).
  Sintaxe: puts(string);
*/

/*
  getchar:
    Essa função é responsável por ler um caractere da entrada padrão.
    Equivale à função putchar, mas em sentido inverso.
  Sintaxe: [variável] = getchar();
*/

/*
  gets:
    Essa função é responsável por ler uma string da entrada padrão. Análoga à função puts, mas em sentido inverso.
  Sintaxe: [variável] = gets();
*/

int main(void) {


  return 0;
}